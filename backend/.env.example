# GEMINI_API_KEY=

GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
GOOGLE_CREDENTIALS_JSON=


DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:6543/postgres
DIRECT_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/postgres

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432
POOLER_PORT=6543

# Auth0 Configuration
AUTH0_DOMAIN=dev-84dj3buehagbf2zt.us.auth0.com
AUTH0_AUDIENCE=https://reverie-api.com

# Auth0 Management API (M2M app — used by scripts/seeding.py only)
AUTH0_MGMT_CLIENT_ID=
AUTH0_MGMT_CLIENT_SECRET=

# Comma-separated browser origins allowed to call this API (CORS)
CORS_ORIGINS=http://localhost:3000,https://<your-frontend-domain>

# Unifold is local-sandbox only (no live payments). Marketplace buy flow
# uses create_deposit_session + confirm-payment — leave these blank.
# UNIFOLD_API_URL=
# UNIFOLD_API_KEY=
# UNIFOLD_WEBHOOK_SECRET=

# Public URL of this backend (optional; used only if you wire real webhooks later)
APP_BASE_URL=http://localhost:8000
